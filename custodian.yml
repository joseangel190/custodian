policies:
  - name: all-rg
    description: List-All
    resource: azure.resourcegroup
    filters:
      - type: value
        key: name
        value: example 
        op: contains
    actions:
      - type: delete
